# Release artifacts

- **`PalworldServerManager.exe`** — Windows portable (double-click). Built with electron-builder.

Rebuild from source:

```bash
cd ../app
npm install
npm run dist
```

Tracked with Git LFS.
